docker tensorflow:
	docker run --runtime=nvidia -it --rm -v $PWD:/tmp -w /tmp tensorflow/tensorflow:latest-gpu python ./test.py

send files to the server:
	scp running.txt  host:/path/running.txt

to run saving log in nohup.out and disassociate from terminal section:
	nohup python2 train.py & disown
to get terminal logs:
	tail -f nohup.out
